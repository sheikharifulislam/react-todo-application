import React from "react";

const Heading = () => {
    return (
        <div className="mt-4 flex justify-center">
            <h1 className="text-[32px] font-semibold">Todo Application</h1>
        </div>
    );
};

export default Heading;
